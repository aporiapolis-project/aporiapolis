"""Configs machine par source externe AporiaPolis.

Les YAML de ce sous-package sont déclarés comme ``package-data`` dans
``pyproject.toml`` et chargés via ``importlib.resources.files()`` pour
portabilité editable / wheel.
"""
